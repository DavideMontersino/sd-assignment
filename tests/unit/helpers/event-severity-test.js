import { eventSeverity } from 'sd/helpers/event-severity';
import { module, test } from 'qunit';

module('Unit | Helper | event severity');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = eventSeverity([42]);
  assert.ok(result);
});
